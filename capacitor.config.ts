import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'u21442453_HW02',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
